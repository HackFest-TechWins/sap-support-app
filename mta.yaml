_schema-version: '3.3'
ID: sap-btp-support-app
version: 1.0.0
description: "SAP BTP Support Application"

modules:
  - name: sap-btp-support-app-srv
    type: nodejs
    path: srv
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: sap-btp-support-app-db

resources:
  - name: sap-btp-support-app-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
